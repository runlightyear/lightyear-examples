import "./restfulApiNoAuth";

// Create a custom app named "openWeatherMap" before uncommenting
import "./restApiWithApiKey";

// Create a custom app named "mySlack" before uncommenting
import "./oauth2";
